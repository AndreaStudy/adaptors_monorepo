export interface PopularCategoryType {
  icon?: string;
  name: string;
  courses: string;
}
export interface PopularCategoryType {
  icon?: string;
  name: string;
  courses: string;
}

export interface PopluarCategoryItemType {
  mentorUuId: string;
  id: number;
  title: string;
  duration: string;
  rating: number;
  reviews: number;
  instructor: string;
}
