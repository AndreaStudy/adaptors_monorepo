export interface PopularCategoryType {
  icon: string;
  name: string;
  courses: string;
}

export interface PopluarCategoryItemType {
  title: string;
  duration: string;
  rating: number;
  reviews: number;
  instructor: string;
}
